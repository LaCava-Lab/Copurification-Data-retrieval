# Copurification-Data-retrieval

1. Clone the repository locally
2. Create the environment with necessary packages and enter a local jupyter notebook instance. Type at  Terminal (Mac OS):
  >`./run_jupyter.sh`
